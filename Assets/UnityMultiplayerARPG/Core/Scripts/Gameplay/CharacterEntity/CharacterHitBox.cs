namespace MultiplayerARPG
{
    /// <summary>
    /// Can use `DamageableHitBox` instead of this class
    /// </summary>
    [System.Obsolete("This is deprecated, use `DamageableHitBox` instead, still keep it for backward compatibility")]
    public class CharacterHitBox : DamageableHitBox
    {
    }
}
